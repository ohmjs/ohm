{
  "private": true,
  "workspaces": ["ohm-js", "examples/ecmascript", "examples/es-module", "examples/operators"],
  "scripts": {
    "build": "yarn workspace ohm-js run build",
    "ci-test": "yarn lint && yarn test",
    "lint": "eslint .",
    "test": "yarn workspaces run test"
  }
}
