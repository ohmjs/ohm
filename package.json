{
  "name": "ohm",
  "version": "0.0.0",
  "description": "an object-oriented language for parsing and pattern matching",
  "keywords": [
    "parser",
    "compiler",
    "pattern matching",
    "pattern-matching",
    "ometa",
    "ometa/js",
    "ometa-js",
    "ometajs",
    "rapid",
    "prototyping"
  ],
  "homepage": "http://tinlizzie.org/ohm",
  "bugs": "http://github.com/alexwarth/ohm/issues",
  "main": "src/main.js",
  "bin": "src/bootstrap.js",
  "scripts": {
    "bootstrap": "npm test --silent && npm run unsafe-bootstrap && (npm test --silent || (echo 'Bootstrap failed.'; mv -v dist/ohm-grammar.js.old dist/ohm-grammar.js))",
    "build-debug": "browserify src/main.js -d -s ohm -o dist/ohm.js",
    "build": "npm run build-debug && uglifyjs dist/ohm.js > dist/ohm.min.js",
    "clean": "rm -f dist/ohm.js dist/ohm.min.js",
    "lint": "eslint src visualizer",
    "test": "mocha",
    "test-watch": "mocha -w",
    "prepublish": "npm run lint && npm run build && npm run bootstrap",
    "postinstall": "bin/dev-setup.sh",
    "unsafe-bootstrap": "cp dist/ohm-grammar.js dist/ohm-grammar.js.old; node src/bootstrap.js src/ohm-grammar.ohm > dist/ohm-grammar.js.new && mv -f dist/ohm-grammar.js.new dist/ohm-grammar.js",
    "visualizer": "bin/ohm-visualizer",
    "watch": "watchify src/main.js -d -s ohm -o dist/ohm.js"
  },
  "license": "MIT",
  "author": "Alex Warth <alexwarth@gmail.com> (http://tinlizzie.org/~awarth)",
  "dependencies": {
    "inherits": "^2.0.1",
    "symbol": ""
  },
  "devDependencies": {
    "browser-sync": "^2.4.0",
    "browserify": "^3.30.4",
    "eslint": "^0.17.1",
    "expect.js": "^0.3.1",
    "mocha": "^1.17.1",
    "uglify-js": "^2.4.12",
    "watchify": "^0.6.1"
  }
}
