{
  "private": true,
  "workspaces": ["ohm-js", "examples/ecmascript", "examples/es-module", "examples/markdown", "examples/operators", "examples/simple-lisp"],
  "scripts": {
    "build": "yarn workspace ohm-js run build",
    "ci-test": "yarn lint && yarn test",
    "lint": "eslint .",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "eslint": "^7.31.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-ava": "^12.0.0",
    "eslint-plugin-camelcase-ohm": "^0.2.1",
    "eslint-plugin-no-extension-in-require": "^0.2.0"
  }
}
