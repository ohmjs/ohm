# ohm-typescript-codegen

⚠️ **WARNING:** Very experimental! ⚠️

Generates .d.ts files from .ohm grammar source files.

## Usage

```
generate-ohm-declarations [<pattern>...]    Generate type declarations for
                                            each of the .ohm files identified
                                            by <pattern>.
```
